"""PLACEHOLDER — DELETE THIS FILE.

Leftover scaffolding from the Opus test gate: a reference
`backfill_recent_repliers` was written here to prove the Task 0 test suite
satisfiable, then removed. The gate's sandbox could not delete the file, so it
is emptied instead.

Task 2 must overwrite this file with the real implementation; until then it
deliberately exports nothing, so `tests/test_store_repliers.py` fails to import
exactly as it did before the gate ran.
"""
